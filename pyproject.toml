[project]
name = "mw-dev-kit-orchestrator"
version = "0.1.0"
description = "Orchestrator for mw-dev-kit instances"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docker>=7.1.0",
    "fastapi[standard]>=0.121.3",
    "pyyaml>=6.0.3",
    "typing-inspect>=0.9.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
