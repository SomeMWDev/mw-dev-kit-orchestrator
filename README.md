# mw-dev-kit-orchestrator

WIP.

This will eventually take over management of multiple [mw-dev-kit](https://github.com/SomeMWDev/mw-dev-kit) instances on
one machine.

Right now, it only acts as a reverse proxy to support custom localhost domains (e.g. `wiki.localhost`).
